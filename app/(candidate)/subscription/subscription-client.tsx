'use client';
import { Check, Crown, Sparkles } from 'lucide-react';
import toast from 'react-hot-toast';
const plans = [{ id: 'free', name: 'Free', price: '$0', items: ['2 text AI interviews', 'Interview feedback', 'Mentor directory'] }, { id: 'standard', name: 'Standard', price: '$9', items: ['10 AI interviews / month', 'Audio interview mode', 'Detailed progress insights'] }, { id: 'premium', name: 'Premium', price: '$19', items: ['Unlimited AI interviews', 'Video avatar interviews', 'Priority support'] }];
export default function SubscriptionClient({ tier, validUntil }: { tier: string; validUntil: string | null }) { return <div className="max-w-5xl mx-auto space-y-7 animate-fade-up"><div><div className="badge-purple mb-3"><Crown className="w-3 h-3" /> Your plan</div><h1 className="text-3xl font-black text-white">Subscription</h1><p className="mt-1">Choose the practice experience that fits your interview goals.</p></div><div className="card p-4 flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs" style={{ color: 'var(--text-muted)' }}>Current plan</p><p className="text-lg font-bold text-white capitalize">{tier}</p></div><p className="text-xs" style={{ color: 'var(--text-secondary)' }}>{validUntil ? `Renews ${new Date(validUntil).toLocaleDateString()}` : 'You can upgrade any time.'}</p></div><div className="grid md:grid-cols-3 gap-5">{plans.map((plan) => <div key={plan.id} className="card rounded-2xl p-6 flex flex-col" style={{ borderColor: plan.id === 'premium' ? 'rgba(0,194,255,.35)' : undefined }}><div className="flex justify-between items-center"><h2 className="text-xl font-bold text-white">{plan.name}</h2>{plan.id === 'premium' && <Sparkles className="w-5 h-5 text-neon-cyan" />}</div><p className="text-3xl font-black text-white mt-4">{plan.price}<span className="text-xs font-normal" style={{ color: 'var(--text-muted)' }}>{plan.id !== 'free' && ' / month'}</span></p><div className="space-y-3 my-6 flex-1">{plan.items.map((item) => <div key={item} className="flex gap-2 text-sm" style={{ color: 'var(--text-secondary)' }}><Check className="w-4 h-4 shrink-0 text-neon-green" />{item}</div>)}</div><button disabled={tier === plan.id} onClick={() => toast('Payments are being enabled soon. Please contact support to upgrade.')} className={tier === plan.id ? 'btn-ghost justify-center opacity-60' : 'btn-neon-green justify-center'}>{tier === plan.id ? 'Current plan' : `Choose ${plan.name}`}</button></div>)}</div></div>; }
